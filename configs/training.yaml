model:
  algorithm: RandomForestClassifier
  params:
    n_estimators: 100
    max_depth: 10
    random_state: 42
  test_size: 0.2

evaluation:
  min_accuracy: 0.80
  min_f1_score: 0.75

mlflow:
  tracking_uri: ./mlruns
  experiment_name: loan_prediction
